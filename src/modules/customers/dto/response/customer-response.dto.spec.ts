import { CustomerResponseDto } from './customer-response.dto';

describe('CustomerResponseDto', () => {
  it('should be defined', () => {
    expect(new CustomerResponseDto()).toBeDefined();
  });
});
